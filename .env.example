# Copy to .env for local runs of today.py (never commit .env).
# In GitHub Actions these come from repo Settings > Secrets and variables > Actions.

# Fine-grained or classic PAT, read-only, all repositories.
ACCESS_TOKEN=your_github_token_here

# Your GitHub username (note: the variable name has an underscore).
USER_NAME=ShayanAkhun
